const dcl = document.addEventListener("DOMContentLoaded", () => {
  const canvas = document.getElementById("game-canvas");
  canvas.width = 500;
  canvas.height = 500;
  const ctx = canvas.getContext("2d");
});

// module.exports = { dcl }; // shortcut for module.exports = { dcl: dcl }
// when key and value are the same, can collapse them into one reference in an object
